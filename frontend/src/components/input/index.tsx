import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const EsInput = () => {
  return (
    <View style={styles.container}>
      <Text>EsInput</Text>
      {/* Je suis a 6h36min21seconde */}
    </View>
  );
};

export default EsInput;

const styles = StyleSheet.create({
  container: {},
});
